
线性表概述：
    线性表的特点：表现了数据元素建的一对一关系
    1、第一个数据元素没有前驱，这个元素叫做头结点
    2、最后一个元素没有后继，这个元素叫做尾结点
    3、除了第一个和最后一个元素外，其他数据元素有且仅有一个前驱和一个后继

线性表的分类：
    1、顺序表
        查询快，增删慢
    2、链表
        查询慢，增删快

快慢指针：
    定义两个指针，这两个指针一快一慢，依次制造出需要的差值，这个哈之可以使我们找到链表上相应的结点
    中间值问题：

循环链表：
    链表整体形成圆环状，在单项链表中，最后一个结点不指向任何一个结点，实现循环链表，要将最后一个结点指向头结点

栈：
    栈是一种先进后出的数据结构，只能从一端进行插入和删除的特殊线性表。

符号表：
    符号表最主要的目的就是将一个键和一个值联系起来，符号表能够将存储的数据元素是一个键个一个值共同的键值对数据，我们可以通过
    键值来寻找对应的值，键值对中，键值需要有唯一性